# 🔥 极端Bug发现测试说明

## 📋 测试配置

### 极端测试参数

- **超时时间**: 60秒（从30秒增加）
- **变异体数量**: 100个/种子（从50个增加）
- **测试种子数**: 480个（所有种子）
- **重构超时**: 120秒（从60秒增加）

### 新增功能

1. **极端变异器** (`extreme_mutator.py`)
   - 极大文件（100MB+）
   - 极深嵌套（1000层+）
   - 极端数值（2^1000等）
   - 恶意Unicode
   - 指数复杂度公式
   - 无限循环模式
   - 恶意结构
   - 极端量词链
   - 空字节注入
   - 格式字符串攻击模式

2. **极端边界情况**
   - 空文件
   - 极大数字字符串
   - 极长字符串
   - 特殊Unicode混合
   - 恶意格式组合

## 🚀 运行方法

### 方法1: 直接运行（前台）

```bash
cd fuzzer
./week8-9_extreme_bug_hunt.sh
```

### 方法2: 后台运行（推荐）

```bash
cd fuzzer
nohup ./week8-9_extreme_bug_hunt.sh > extreme_bug_hunt.log 2>&1 &
```

### 方法3: 使用screen（推荐）

```bash
cd fuzzer
screen -S extreme_test
./week8-9_extreme_bug_hunt.sh
# 按 Ctrl+A 然后 D 来分离会话
# 使用 screen -r extreme_test 重新连接
```

### 方法4: 手动运行

```bash
cd fuzzer
python3 main.py \
    --seed-dir ../sledgehammer_export \
    --output-dir ./week8-9_extreme_bug_hunt \
    --timeout 60.0 \
    --num-mutants 100 \
    --max-seeds 480 \
    --use-ast-mutator \
    --use-reconstruction-oracle \
    --reconstruction-timeout 120.0 \
    --use-aggressive-mutator \
    --use-extreme-mutator \
    --use-parallel \
    --num-workers 4
```

## ⏱️ 预期执行时间

### 估算

- **480个种子** × **100个变异体** = **48,000个变异体**
- **每个变异体** × **2个prover** (Z3 + cvc5) = **96,000个测试**
- **平均每个测试**: 0.03秒（如果正常完成）
- **总执行时间**: 约 **2,880秒** = **48分钟**（如果所有测试都正常完成）

### 实际情况

- 如果有timeout（60秒），时间会显著增加
- 如果有crash，prover重启时间会增加
- 如果有大文件，处理时间会增加
- **实际执行时间可能达到数小时**

## 📊 监控进度

### 方法1: 查看日志

```bash
tail -f week8-9_extreme_bug_hunt/logs/fuzzer.log
```

### 方法2: 查看统计文件

```bash
cat week8-9_extreme_bug_hunt/stats/stats.json | python3 -m json.tool
```

### 方法3: 查看发现的bug

```bash
ls -lh week8-9_extreme_bug_hunt/bug_*.json
ls -lh week8-9_extreme_bug_hunt/differential_*.json
ls -lh week8-9_extreme_bug_hunt/reconstruction_failure_*.json
```

## 🛑 中断和恢复

### 中断测试

- 按 `Ctrl+C` 中断前台运行
- 使用 `kill` 命令停止后台进程

### 恢复测试

- 测试结果会实时保存
- 可以查看已完成的种子数量
- 可以手动从断点继续（需要修改代码）

## ⚠️ 注意事项

1. **磁盘空间**: 确保有足够的磁盘空间（可能产生大文件）
2. **内存**: 大文件和深度嵌套可能消耗大量内存
3. **CPU**: 并行处理会占用多个CPU核心
4. **时间**: 测试可能需要数小时，建议后台运行

## 💡 优化建议

### 如果测试太慢

1. **减少变异体数量**
   ```bash
   --num-mutants 50  # 从100减少到50
   ```

2. **减少种子数量**
   ```bash
   --max-seeds 200  # 从480减少到200
   ```

3. **减少超时时间**
   ```bash
   --timeout 30.0  # 从60秒减少到30秒
   ```

### 如果想要更极端

1. **增加超时时间**
   ```bash
   --timeout 120.0  # 从60秒增加到120秒
   ```

2. **增加变异体数量**
   ```bash
   --num-mutants 200  # 从100增加到200
   ```

3. **禁用并行处理**
   ```bash
   # 移除 --use-parallel 参数
   ```

## 📈 结果分析

测试完成后，结果会保存在 `week8-9_extreme_bug_hunt/` 目录：

- `bug_*.json` - Crash/Timeout Bug报告
- `differential_*.json` - Differential Bug报告
- `reconstruction_failure_*.json` - Reconstruction Failure报告
- `stats/stats.json` - 统计信息
- `logs/fuzzer.log` - 日志文件

详细分析请参考 `BUG_FINDING_RESULTS_ANALYSIS.md`。

