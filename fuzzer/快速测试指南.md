# 🚀 Fuzzer快速测试指南

## 📋 测试步骤

### 1. 测试单个组件

```bash
# 测试TPTP解析器
cd fuzzer
python3 parser/tptp_parser.py

# 测试Token变异器
python3 mutator/token_mutator.py

# 测试Crash Oracle
python3 oracle/crash_oracle.py

# 测试Differential Oracle
python3 oracle/differential_oracle.py
```

### 2. 运行完整Fuzzer（小规模测试）

```bash
cd fuzzer

# 小规模测试（2个种子，每个3个变异体）
python3 main.py --max-seeds 2 --num-mutants 3 --timeout 3.0
```

### 3. 查看结果

```bash
# 查看输出目录
ls -lh fuzzer_results/

# 查看bug报告
cat fuzzer_results/bug_*.json

# 查看差异报告
cat fuzzer_results/differential_*.json
```

## ⚠️ 注意事项

1. **确保provers可用**:
   - Z3: `z3 --version`
   - cvc5: `cvc5 --version`

2. **种子文件存在**:
   - 默认种子目录: `../sledgehammer_export/`
   - 应包含`.p`文件

3. **执行时间**:
   - 每个变异体可能需要几秒钟
   - 建议先用小规模测试

## 🐛 常见问题

### 问题1: Import错误
```bash
# 确保在fuzzer目录中运行
cd fuzzer
python3 main.py
```

### 问题2: Prover未找到
```bash
# 检查prover是否在PATH中
which z3
which cvc5

# 如果不在PATH中，添加到PATH或修改代码中的prover路径
```

### 问题3: 种子文件不存在
```bash
# 检查种子目录
ls -lh ../sledgehammer_export/*.p | head -5

# 如果为空，先运行Sledgehammer导出一些文件
```

## ✅ 预期结果

成功运行后，应该看到：

1. **控制台输出**:
   - 处理进度
   - 发现的bug（如果有）
   - 统计信息

2. **输出目录** (`fuzzer_results/`):
   - `bug_*.json` - Bug报告
   - `differential_*.json` - 差异报告

3. **统计信息**:
   - 总测试数
   - 崩溃数
   - 超时数
   - 差异数

